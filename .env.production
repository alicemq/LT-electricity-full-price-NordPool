# Production Environment Configuration
NODE_ENV=production

# Frontend Configuration
VITE_API_BASE_URL=/api/v1
FRONTEND_URL=http://localhost:80

# Backend Configuration
BACKEND_COMMAND=node src/index.js

# Frontend Configuration
FRONTEND_COMMAND=nginx -g "daemon off;"

# Data Sync Configuration
DATA_SYNC_COMMAND=node src/sync.js
WORKER_COMMAND=node src/worker.js

# Database Configuration (internal only)
# POSTGRES_DB=electricity_prices
# POSTGRES_USER=electricity_user
# POSTGRES_PASSWORD=electricity_password

# Security: Backend and database not exposed to internet
# All API access goes through frontend proxy 